<div class="col-7">
  <div class="row">
    <h3>ジャンル一覧・追加</h3>
  </div>

  <div class="row mt-5">
    <div class = "col">
    <%= form_with model: @genre, url: admin_genres_path, local:true do |f| %>
      <div class="form-group row">
        <%= f.label :ジャンル名, class: "col-sm-3" %>
        <%= f.text_field :name, class: "form-control col-sm-4" %>
        <%= f.submit :新規登録, class: "col-sm-3 ml-5 btn btn-light" %>
      </div>
    <% end %>
    </div>
  </div>


  <div class="row mt-5">
    <div class = "col-6">
      <table class="table table-hover">
        <thead>
          <tr>
            <th>ジャンル</th>
            <th></th>
          </tr>
        </thead>

        <tbody>
          <% @genres.each do |genre| %>
            <tr>
              <td><%= genre.name %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>